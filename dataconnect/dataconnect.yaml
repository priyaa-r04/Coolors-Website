specVersion: "v1"
serviceId: "coolors-webapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "coolors-webapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
